<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main Vulcan theme — AMOLED dark, no action bar (Compose handles UI) -->
    <style name="Theme.Vulcan" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/vulcan_deep_forge</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>

    <!-- Translucent theme for SlotActivity — appears as overlay briefly -->
    <style name="Theme.Vulcan.Translucent" parent="Theme.Vulcan">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>

    <!-- Splash screen theme (used before Compose loads) -->
    <style name="Theme.Vulcan.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/vulcan_deep_forge</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_vulcan_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Vulcan</item>
    </style>
</resources>
